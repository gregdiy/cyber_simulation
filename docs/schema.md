# ENTERPRISE SECURITY LOG DATASET — SCHEMA DOCUMENTATION  
**Dec 2025 – Jan 2026 | Multi-User Pivot Living-Off-The-Land Campaign | ATK_50673**

---

## OVERVIEW

This dataset contains **realistic enterprise security logs** with an embedded **multi-stage attack campaign** that pivots across identities and privilege levels.

Logs include:

- **Windows security events** (process execution, authentication, network connections, file operations, system activity)
- **Defense / observability logs** (EDR, DLP, SIEM, PAM, MFA-style alerts/blocks/denials)
- **Service account background activity** (continuous automation noise at enterprise scale)
- **Event-level ground truth** for attacker actions (`attack_id`)

**Generation model (high-level):**
- Both **benign** and **attack** activity are generated by an AI-driven simulation framework.
- The attacker is guided by a **100+ parameter defensive environment** (controls, coverage, enforcement, alert overlap/noise) and optimizes its behavior under that posture.

### Key characteristics (this release)

- **7,961,002 total logs** spanning **25 days**
- **345 attacker actions** (Windows attacker-action telemetry with `attack_id != null`)
- **142,454 defense/observability logs** (alerts/blocks/denials/correlations)
- **285 attack-triggered observability events**  
  > Note: this is **not de-duplicated**; a single attacker action can generate multiple observability events (different tools / different alert types).

---

## LOG TYPE CATEGORIES

All logs are categorized by `log_type`.

### 1) Windows Security Events
- `windows_security_event` — primary Windows telemetry for all identities (users + service accounts)

### 2) Defense / Observability Logs
Defense logs represent tool outputs (alerts, blocks, denials, correlations). Common examples:

- **EDR / Endpoint:** `defender_atp_alert`, `defender_atp_detection`, `amsi_detection`, `memory_protection_block`, `credential_guard_block`
- **Identity / Privilege:** `pam_access_denied`, `credential_access_alert`, `access_control_event`, `kerberos_attack_detected`
- **Network:** `proxy_block`, `firewall_block`, `dns_query_alert`, `smb_traffic_alert`, `lateral_movement_alert`
- **Data protection:** `dlp_alert`, `dlp_block`
- **Correlation / analytics:** `siem_alert`, `behavioral_detection_alert`, `suspicious_command_line_detected`
- Additional observability types may appear as the simulated defensive stack expands.

---

## CORE FIELDS (ALL LOGS)

These fields are either always present or commonly present across all log families.

| Field | Type | Required | Description |
|---|---|---:|---|
| `timestamp` | string | yes | ISO 8601 timestamp (includes timezone), e.g. `2025-12-29T23:12:04.000-08:00` |
| `log_type` | string | yes | Log family (see categories above) |
| `event_type` | string | yes | High-level event type (see Event Types section) |
| `account` | string | yes | Security principal performing the action: user (`daniel.davis004`) or service account (`svc_*`) |
| `user` | string/null | conditional | Human identity context when available (often set for user-driven activity; may be null for some defense logs / pure service account activity) |
| `service_account` | boolean | yes | `true` if `account` is a service account (`svc_*`), else `false` |
| `hostname` | string | yes | Device name: `WS-SAL-0005`, `WS-IT-0063`, `DB-SRV-02`, etc. |
| `device_type` | string | yes | e.g. `workstation`, `database_server`, `domain_controller`, `web_server`, `app_server` |
| `location` | string | yes | `NYC_HQ`, `SF_Office`, `London`, `Remote_VPN` |
| `department` | string | conditional | Department label for user identities (e.g. `Sales`, `IT`, `Finance`, etc.) |
| `process_name` | string | conditional | Executable/process name: `powershell.exe`, `cmd.exe`, etc. |
| `parent_process` | string | conditional | Parent process name: `explorer.exe`, `cmd.exe`, etc. |
| `command_line` | string | conditional | Full command line with arguments |
| `source_ip` | string | conditional | Source IP (internal or VPN range), e.g. `10.3.116.100` |
| `destination_ip` | string | conditional | Destination IP (internal or external), e.g. `10.x.x.x` or `203.0.113.x` |
| `port` | integer | conditional | Network port: `88`, `443`, `3389`, `8443`, etc. |
| `protocol` | string | conditional | e.g. `TCP`, `UDP`, `HTTPS`, `RDP`, `Kerberos`, `SMB`, `WinRM` |
| `session_id` | string | conditional | Session / activity grouping identifier |
| `success` | boolean | conditional | Success indicator (primarily for Windows events; may appear on some defense logs) |
| `error` | string | conditional | Error string if `success=false` |
| `event_id` | integer | conditional | Windows Event ID when applicable (e.g. 4688, 4625, 5156, 4769) |

### Key Insight: `user` vs `account`
The `account` field answers: **whose credentials performed the action**.

- `user=daniel.davis004`, `account=daniel.davis004` → direct user activity  
- `account=svc_backup` → service account automation (or credential misuse if later labeled with `attack_id`)  
- Some defense logs may omit `user` and still include `account` as the primary identity.

---

## DEFENSE / OBSERVABILITY FIELDS (DEFENSE LOGS ONLY)

These fields appear on defense logs (when `log_type != windows_security_event`). Some may also appear on Windows events if the event includes file hashing or extended context.

### Detection Core

| Field | Type | Required | Description |
|---|---|---:|---|
| `severity` | string | yes | `low`, `medium`, `high`, `critical` |
| `action_taken` | string | yes | `logged`, `alerted`, `blocked`, `denied`, `quarantined`, `isolated` (varies by log_type) |
| `alert_name` | string | yes | Human-readable alert/detection name |
| `alert_type` | string | conditional | e.g. `threat`, `behavioral`, `policy`, `rule` |
| `reason` | string | conditional | Explanation / rationale for alert/deny/block |
| `confidence_level` | float | conditional | 0.0–1.0 confidence score (tool/model confidence) |
| `vendor` | string | conditional | Defense product / control vendor name |

### Access / Authentication Context

| Field | Type | Required | Description |
|---|---|---:|---|
| `authentication_type` | string | conditional | e.g. `Kerberos`, `NTLM`, `OAuth`, `Certificate`, `Service Account Authentication` |
| `policy_violated` | string | conditional | Policy name violated (common on DLP/PAM/access-control events) |
| `target` | string | conditional | Targeted asset/resource: `lsass.exe`, `Credential Store`, etc. |
| `target_account` | string | conditional | Account that was targeted / requested / attempted (common on PAM/access-control events) |

### Network / Specialized Context

| Field | Type | Required | Description |
|---|---|---:|---|
| `http_status` | string | conditional | HTTP status for proxy/network events (e.g. `403 Forbidden`) |
| `query_type` | string | conditional | DNS query type (`A`, `AAAA`, `MX`, etc.) |

> Notes:
> - Defense logs may include `command_line`, `process_name`, `hostname`, `source_ip`, `destination_ip` when the tool attaches the triggering context.
> - Some defense logs include `technique` (see Attack Labels section).

---

## FILE HASH & REPUTATION FIELDS

Hashing fields appear only on events where the simulated stack “decides” to hash an artifact (commonly `process_start`, file creation/modification events, and select network/file events).

| Field | Type | Required | Description |
|---|---|---:|---|
| `sha256` | string | conditional | 64-char SHA-256 hash |
| `md5` | string | conditional | 32-char MD5 hash |
| `file_size` | integer | conditional | File size in bytes |
| `file_path` | string | conditional | Full file path, e.g. `C:\\temp\\archive.zip` |
| `signed` | boolean | conditional | Whether the artifact is digitally signed |
| `prevalence_score` | float | conditional | Approximate prevalence within the enterprise (0.0–1.0) |

**Intended semantics**
- Known Windows binaries reuse stable hashes across events within a run.
- Attacker-created scripts/archives often have rare hashes / low prevalence scores.
- `signed=true` is more common for typical enterprise binaries than for attacker artifacts.

---

## ATTACK LABELS (GROUND TRUTH)

Ground truth is expressed via the `attack_id` field.

| Field | Type | Required | Description |
|---|---|---:|---|
| `attack_id` | string/null | yes | `ATK_50673` or null |
| `attack_type` | string/null | conditional | MITRE technique ID (e.g. `t1059.001`) when populated |
| `technique` | string/null | conditional | Optional alias for technique labeling (may mirror `attack_type` on some defense logs) |
| `stage_number` | string/null | yes | `"0"`–`"15"` for staged attacker actions, else null |

- **Benign events:** `attack_id`, `attack_type`, `stage_number` are null  
- **Attacker actions:** `attack_id` is non-null, with stage/technique metadata populated where applicable  
- **Attack-linked defense logs:** some observability events include `attack_id`/`stage_number` to indicate linkage to attacker actions

### Kill chain mapping (ATK_50673)

Stages map to techniques as follows:

- **Stages 0–3:** `t1059.001` — PowerShell execution / initial foothold & probing  
- **Stages 4–6:** `t1087.002` — Discovery (domain / account discovery)  
- **Stages 7–10:** `t1021.001` — Remote Services / lateral movement (RDP/WinRM patterns)  
- **Stages 11–15:** `t1041` — Exfiltration over C2 channel

### Identity progression (multi-user pivot)

This campaign pivots across privilege contexts:

- `daniel.davis004` (Sales user, initial compromise)
- `sarah.wilson062` (IT user, normal account; includes both benign admin work and attack-linked events in the timeline)
- `sarah.wilson062_admin` (IT admin account; attacker actions blend with legitimate admin patterns)

> Important: An attacker can continue using earlier credentials while also operating under a newly obtained privileged account. This is reflected by attacker actions (`attack_id != null`) appearing under multiple accounts during later stages.

---

## EVENT TYPES

### Common Windows Security Event Types (`windows_security_event`)
Examples observed in this dataset include:

- `process_start`
- `network_connection`
- `file_access`, `file_create`, `file_modify`, `file_delete`
- `admin_action`
- `system_event`
- `kerberos_auth_success`, `kerberos_auth_failure`
- `kerberos_service_ticket_success`
- `ntlm_auth_success`, `ntlm_auth_failure`
- `oauth_token_success`, `oauth_token_failure`
- `certificate_auth_success`, `certificate_auth_failure`

### Defense / Observability Event Types (varies by `log_type`)
Defense logs often reuse the same `event_type` categories (for context) or use control-specific types. Example: PAM denials, DLP blocks, proxy blocks, SIEM correlations, etc.

---

## LOG TYPE REFERENCE (COMMON IN THIS RELEASE)

### Windows
- `windows_security_event`

### EDR / Endpoint
- `defender_atp_alert`
- `defender_atp_detection`
- `amsi_detection`
- `memory_protection_block`
- `credential_guard_block`

### Identity / Privilege
- `pam_access_denied`
- `credential_access_alert`
- `access_control_event`
- `kerberos_attack_detected`

### Network
- `proxy_block`
- `firewall_block`
- `dns_query_alert`
- `smb_traffic_alert`
- `lateral_movement_alert`

### Data Protection
- `dlp_alert`
- `dlp_block`

### Correlation / Analytics
- `siem_alert`
- `behavioral_detection_alert`
- `suspicious_command_line_detected`

### Other
- `nac_quarantine`
- `network_event`

---

## MITRE ATT&CK TECHNIQUE REFERENCE (USED IN THIS RELEASE)

- **t1059.001** — Command and Scripting Interpreter: PowerShell  
  https://attack.mitre.org/techniques/T1059/001/

- **t1087.002** — Account Discovery: Domain Account  
  https://attack.mitre.org/techniques/T1087/002/

- **t1021.001** — Remote Services: Remote Desktop Protocol  
  https://attack.mitre.org/techniques/T1021/001/

- **t1041** — Exfiltration Over C2 Channel  
  https://attack.mitre.org/techniques/T1041/

---

## EXAMPLE LOGS

### Example 1: Benign User Event — Process Start (Windows)
```json
{
  "timestamp": "2025-12-22T09:12:00.000-08:00",
  "log_type": "windows_security_event",
  "event_type": "process_start",
  "user": "daniel.davis004",
  "account": "daniel.davis004",
  "service_account": false,
  "hostname": "WS-SAL-0005",
  "device_type": "workstation",
  "department": "Sales",
  "location": "SF_Office",
  "process_name": "teams.exe",
  "parent_process": "explorer.exe",
  "command_line": "C:\\Program Files\\Teams\\current\\Teams.exe",
  "source_ip": "10.2.115.23",
  "success": true,
  "event_id": 4688
}
Example 2: Attacker Action — PowerShell Admin Action (Windows)
{
  "timestamp": "2025-12-21T06:14:03.000-08:00",
  "log_type": "windows_security_event",
  "event_type": "admin_action",
  "user": "daniel.davis004",
  "account": "daniel.davis004",
  "service_account": false,
  "hostname": "WS-SAL-0005",
  "device_type": "workstation",
  "department": "Sales",
  "location": "SF_Office",
  "process_name": "powershell.exe",
  "parent_process": "cmd.exe",
  "command_line": "Get-ScheduledTask | Where-Object {$_.State -eq 'Running'}",
  "source_ip": "10.2.115.23",
  "success": true,
  "attack_id": "ATK_50673",
  "attack_type": "t1059.001",
  "stage_number": "0"
}
Example 3: Attack-Linked Defense Event — PAM Access Denied (Defense Log)
{
  "timestamp": "2025-12-29T23:12:04.000-08:00",
  "log_type": "pam_access_denied",
  "event_type": "admin_action",
  "hostname": "WS-IT-0063",
  "account": "sarah.wilson062",
  "user": "sarah.wilson062",
  "source_ip": "10.3.116.100",
  "command_line": "invoke-command -scriptblock {get-date}",
  "severity": "high",
  "reason": "Unauthorized service account access attempt",
  "action_taken": "denied",
  "authentication_type": "Service Account Authentication",
  "target_account": "daniel.davis004",
  "attack_id": "ATK_50673",
  "attack_type": "t1087.002",
  "stage_number": "6"
}
KEY NOTES
String / null formatting

Booleans are native JSON booleans (true / false)

Stage numbers are stored as strings ("0", "1", …)

Null values are JSON null (missing optional fields may be absent or null)

Defense log behavior

Defense/observability logs are generated as reactions to activity (benign or attacker).

Multiple observability events may trigger on a single attacker action.

Some defense logs include attack_id/stage_number when linked; many benign-triggered observability events have null attack_id.

Network patterns

Internal enterprise addressing: 10.x.x.x

Remote/VPN addressing (when present): 192.168.x.x

External endpoints use documentation ranges (e.g., 203.0.113.x) for realism without real infrastructure.

Service account realism

Service accounts (svc_*) generate continuous high-volume activity.

Malicious use of service accounts can be represented by pairing unusual context with attack_id labeling (where applicable in a scenario).

DETECTION CHALLENGES (WHY THIS SCHEMA MATTERS)

This dataset is designed to capture real detection failure modes:

Tool overlap: attacker uses the same tooling that admins and IT staff use.

Privilege transitions: the pivot into IT and IT-admin context is where naive detectors break.

SOC noise: observability logs are high-volume and overlapping—exactly what defenders deal with.

Multi-week timeline: actions occur over weeks, avoiding spike-based anomaly assumptions.

Event-level ground truth: supports precise benchmarking and recall-by-stage analysis.